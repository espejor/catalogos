<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragment_auctions_house"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:tag="fragment_auctions_house"
    tools:context=".auctions_house_package.auctions_house.AuctionsHouseFragment">

    <ListView
        android:id="@+id/auctions_house_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        tools:listitem="@layout/list_item_auction_house" />
    <TextView
        android:id="@+id/text_empty_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="invisible"
        android:text="@string/empty_list"
        android:gravity="center"/>

</FrameLayout>
